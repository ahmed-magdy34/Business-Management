import dashboard from './dashboard';
import tasks from './utilities';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, tasks] //,pages]
};

export default menuItems;
